# Fog
Browser extension: copper control chip per tab. Left (silver) knob dims/brightens
the page; right (gold) knob lowers that tab's audio. Toolbar icon toggles globally.
Built by Railroad Earth ATL.

## Install (unpacked)
chrome://extensions → Developer mode ON → Load unpacked → select this folder.
